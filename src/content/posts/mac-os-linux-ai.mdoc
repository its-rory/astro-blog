---
title: 从 macOS 到 Linux：AI 语音知识库《乱写》项目的全平台适配、子卡片架构重构与部署踩坑实录
publishedDate: 2026-07-22
---
## 前言

最近在 GitHub 上发现了一个非常惊艳的开源 AI 语音笔记项目——**《乱写 (Luanxie)》**。它的核心逻辑很有意思：**无脑丢进语音、文字、照片，AI 自动在后台帮你过滤语气词、修正错别字，并渐进式地整理成结构化的 Markdown 笔记，最后原子化增量导出到你本地的 Obsidian 知识库。**

但原项目在实际应用和多平台部署中存在两个主要痛点：

1. 平台限制：它默认是为 macOS (Apple Silicon) 编写的。音频本地转写深度绑定了 Apple 的 mlx-whisper，后台开机自启使用 Mac 的 .plist。
1. 长文本重写的合并幻觉：旧版本每次有新碎片加入主题时，系统会调用高级 LLM 将新数据全量重写并入一个大 Markdown 笔记中。随着字数增多，LLM 极易在重写过程中漏掉历史记录，并且产生幻觉。

为了克服这些局限，我针对该项目进行了**全平台适配重构**、**升级了全新的子卡片流式关联体系**，并成功在 Linux VPS 服务器上实现了 24 小时常驻私有化部署。

本文将手把手记录下这次源码重构方案、Linux 部署流程，以及一路上遇到的各种经典“大坑”和对应解法，供大家参考，方便大家直接复制发布或用于技术分享。

---

## 一、 源码重构方案与架构演进

### 1. 依赖按平台条件安装 (pyproject.toml)

利用 PEP 508 环境变量标记，在打包构建时，自动根据当前运行平台进行依赖选择。这样在 Linux 上就不会因为强行安装 macOS 独占的 mlx-whisper 而报错：

```

dependencies = [

    "anthropic>=0.116.0",

    "fastapi>=0.139.0",

    "httpx>=0.27.0",

    "openai>=1.50.0",

    # macOS Apple Silicon 环境安装 mlx-whisper

    "mlx-whisper>=0.4.3; sys_platform  'darwin' and platform_machine  'arm64'",

    # 其他环境（Linux/Windows/Intel Mac）自动安装 faster-whisper

    "faster-whisper>=1.0.0; sys_platform != 'darwin' or platform_machine != 'arm64'",

    "pydantic>=2.13.4",

    "python-dotenv>=1.2.2",

    "python-multipart>=0.0.32",

    "uvicorn[standard]>=0.50.2",

]


```

### 2. 跨平台音频转写模块与预压缩 (transcribe.py)

- 本地转写：支持 mlx-whisper -> faster-whisper 自动降级识别。

- FFmpeg 强力预转码压缩：

  在调用任何云端 API 前，系统会自动调用 ffmpeg，将浏览器录制的各种格式音频（如 webm, m4a）统一转换为 16kHz 单声道 64kbps 的极轻量 .mp3 文件。这不仅解决了模型不识别 webm 导致 API 报错的问题，还将上传体积压缩了 5~10 倍，节约流量且极大缩短了请求时间。

### 3. LLM 结构化输出适配层 (llm.py)

- 抽象封装 call_structured，支持 Anthropic 与 OpenAI 协议接口的无缝切换，方便直接使用 DeepSeek 系列模型。
- 自动转换 Payload：将 Anthropic 的多段系统提示列表（System List）转换为 OpenAI 兼容的单条 System 消息；自动将 Anthropic 专属的 Image base64 结构翻译为 OpenAI 的 image_url。

### 4. 终极架构改造：子卡片流式关联体系

为了彻底消除大模型长文本重组合并带来的“信息丢失”与“AI 幻觉”，我将合并逻辑重构为**多对一的子卡片关联链**：

- 大主题（Topic）：仅负责存储标题、分类标签和动态摘要，不再包含易被 AI 篡改的长篇 body_md。
- 子卡片（Capture）：每次丢进来的碎片独立存储。每张子卡片完整保留了它自己的**原始附件（音频/图片）、转写原文、AI解析内容（支持前端自动渲染无序列表）和精准到分钟的创建时间**。
- 独立的版本历史与一键回退：在数据库增设 capture_versions 历史快照表，支持在 UI 中直观地查看每张子卡片的**历史编辑快照、逐行 Diff 差异对比，并支持一键 rollback（回滚）**，实现了 100% 的信息防丢失。

### 5. 智能体验：合并 AI 自动命名机制

我们Repurposed了配置中的 “合并 AI” (Merge AI) 大模型（如 DeepSeek-V3 级别），由其在后台异步执行结构化命名：

- 自动生成子卡片标题：Merge AI 从净化后的内容中提炼出 4~12 字的专业短语（如“一二期分拣机高度差”），并展示在子卡片头部。
- 自动生成主题卡片标题：当分类阶段判定需要新建大主题时，由 Merge AI 自动生成一个高度概括的主题名（如“分拣工程规划”）。
- 用户可在 UI 编辑器中随时手动修改子卡片标题，修改将自动记录入版本历史中。

---

## 二、 Linux 服务器部署环境要求

{% table %}
- 依赖/工具
- 作用
- 安装方法 (以 Ubuntu 为例)
---
- Node.js 18+
- 构建 React 前端静态网页
- sudo apt install -y nodejs npm
---
- Python 3.12
- 运行后端的核心运行时 (避开过新的 3.14 以免依赖库编译卡死)
- 使用 uv 自动安装
---
- uv
- 极速 Python 包与虚拟环境管理器
- curl -LsSf [https://astral.sh/uv/install.sh](https://astral.sh/uv/install.sh)
---
- ffmpeg
- 语音转写前置音频处理与转码工具
- sudo apt install -y ffmpeg
{% /table %}

---

## 三、 详细部署步骤

### 1. 获取代码与配置

```

git clone https://github.com/its-rory/luanxie.git /opt/luanxie

cd /opt/luanxie

# 从模板复制并配置 .env 环境变量

cp .env.example .env

nano .env


```

在 .env 中根据您的 API Key 填入配置，以下是使用**硅基流动（SiliconFlow）**接入 DeepSeek 系列模型的推荐配置：

```

# 指定接口类型为 openai

LLM_PROVIDER=openai

# 填写 API Key 及 URL

OPENAI_API_KEY=sk-xxxx...

OPENAI_BASE_URL=https://api.siliconflow.cn/v1

# 语音转文字 API 配置

TRANSCRIPTION_API_KEY=sk-xxxx...

TRANSCRIPTION_BASE_URL=https://api.siliconflow.cn/v1

# 推荐使用 SenseVoice，转写速度极快且对中文非常友好

TRANSCRIPTION_MODEL=FunAudioLLM/SenseVoiceSmall

# 分类与命名大模型自定义

CLASSIFY_MODEL=deepseek-ai/DeepSeek-V3

MERGE_MODEL=deepseek-ai/DeepSeek-V3  # 自动命名采用该高阶大模型


```

### 2. 构建前端静态文件

由于仓库未打包前端静态编译资源，首次部署必须打包编译：

```

cd /opt/luanxie/web

npm install

npm run build

cd ..


```

构建成功后，将在 web/ 下生成 dist/ 静态资源目录。

### 3. 初始化 Python 3.12 并测试运行

使用 uv 锁死成熟稳定的 Python 3.12 虚拟环境，并使用阿里云 PyPI 镜像源以防安装卡死：

```

# 1. 删除不兼容的旧虚拟环境

rm -rf .venv

# 2. 指定 Python 3.12 创建全新的虚拟环境

uv venv --python 3.12

# 3. 指定国内源前台测试运行

UV_INDEX_URL=https://mirrors.aliyun.com/pypi/simple/ uv run --python 3.12 uvicorn server.main:app --host 0.0.0.0 --port 8787


```

看到提示 Uvicorn running on [http://0.0.0.0:8787](http://0.0.0.0:8787) 时表示启动成功，按 Ctrl + C 退出。

### 4. 配置 Systemd 后台常驻服务

1. 打开并编辑 /opt/luanxie/scripts/luanxie.service，确认绝对路径正确。
1. 运行以下命令，完成服务注册与开机自启：

```

# 1. 建立全局软链接，确保 Systemd 能找到 uv 命令

sudo ln -sf /root/.local/bin/uv /usr/local/bin/uv

sudo ln -sf /root/.local/bin/uvx /usr/local/bin/uvx

# 2. 复制服务配置文件

sudo cp /opt/luanxie/scripts/luanxie.service /etc/systemd/system/

# 3. 重载并开启后台守护服务

sudo systemctl daemon-reload

sudo systemctl enable --now luanxie

# 4. 确认服务状态为绿色的 active (running)

sudo systemctl status luanxie


```

### 5. 域名与 HTTPS 证书配置 (手机录音刚需)

根据浏览器的安全限制，**手机端（如 iOS Safari）只有在 HTTPS 协议下才被允许调用麦克风录音**。

如果使用 1Panel 面板，可以轻松配置：

1. 解析域名：将您的二级域名（如 luanxie.domain.com）解析到服务器公网 IP。
1. 反向代理：进入 1Panel ➡️ 网站 -> 创建网站 -> 反向代理，主域名填入解析的二级域名，代理地址填入本地监听端口 127.0.0.1:8787。
1. 启用证书：点击网站设置 ➡️ HTTPS，启用证书并将申请验证方式选择 HTTP 自动（1Panel 会基于 Let's Encrypt 自动完成证书申领和续签）。

---

## 🔥 踩坑记录与解决方案 (Troubleshooting)

### 坑 1：Systemd 服务启动时报 Status 127 找不到 uv

- 原因：uv 默认被安装在了当前用户的私有目录 ~/.local/bin 下。而 Systemd 后台服务运行时使用的是干净、隔离的环境变量，其默认的 PATH 中不包含用户的私有 bin 目录。
- 解决：将 uv 和 uvx 软链接到系统全局 PATH 目录 /usr/local/bin（参考部署步骤第 4 步）。

### 坑 2：系统默认 Python 3.14 导致 llvmlite 编译卡死

- 原因：服务器的系统 Python 版本如果是太新的版本（如 3.14），许多偏向底层的 C++ 库没有预编译发布对应的二进制包（Wheel）。系统会自动退回到源码编译，在 VPS 上现场编译非常消耗 CPU 和内存，极易卡死。
- 解决：强制 uv 使用更稳定成熟的 Python 3.12 运行。删除 .venv，并执行 uv venv --python 3.12 重新创建。

### 坑 3：Systemd 启动提示 Waiting to acquire exclusive lock 无限排队

- 原因：之前前台运行的进程在 Ctrl+C 强退时没有退出干净（留下了 Python 僵尸进程），或者之前强制中断时锁文件未自动删除，导致锁被抢占。

- 解决：强行杀掉全部后台残留进程，并物理删除锁文件：

  ```
  
  sudo systemctl stop luanxie
  
  sudo pkill -9 -f uv
  
  sudo pkill -9 -f python
  
  rm -f /opt/luanxie/.venv/.lock
  
  sudo systemctl start luanxie
  
  
  ```

### 坑 4：手机端连接系统后显示空白，无法上传语音

- 原因：当前使用的是不安全的 HTTP 连接，手机浏览器出于安全策略，禁用了 Web Audio API 和录音组件权限。
- 解决：请配置有效的二级域名解析并开启 HTTPS 证书访问。

### 坑 5：列表圆点超出卡片边缘

- 原因：前端 markdown 解析输出在子卡片 .sub-card 容器下的样式未正确继承，无序列表（ul, ol）缩进丢失。
- 解决：在 styles.css 中追加 .sub-card ul 和 .sub-card ol 的 padding-left: 22px 规则，修复样式排版错乱。

---

## 结语

通过本次全平台适配与**全新子卡片流式关联架构**重构，彻底移除了高延迟且易产生幻觉的文本合并阶段。配合 DeepSeek-V3 极速的提炼分析和 Merge AI 的自动卡片命名，每一条随手丢进来的想法都在收件箱被规整为清晰、独立、可溯源的子记录，为知识管理与个人博客发布带来了质的飞跃。

如果您也有私有化部署个人 AI 语音知识库的需求，不妨也尝试在自己的服务器上搭建一个吧！
