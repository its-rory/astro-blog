---
title: Clash Party TUN 模式断网修复
publishedDate: 2026-07-15
---
> **文档状态**：已解决（通过开机自动静默修复计划任务）\
**最后更新时间**：2026-07-15

---

## 1. 现象与痛点

在 Windows 11 系统上使用 **Clash Party (Mihomo 内核)** 开启 **TUN 模式**后，整机网络断开，无法上网。

- **临时解决方法（Workaround）**：进入“控制面板 -> 网络连接”，找到物理网卡（如以太网），右键属性 -> 共享，勾选一次“允许其他网络用户通过此计算机的 Internet 连接来连接”并保存，然后再取消勾选并保存（即**开关一次 ICS 共享**）。网络即可恢复。
- **痛点**：每次重启电脑或重启 Clash Party TUN 后，都必须手动重复上述繁琐步骤，用户体验极差。

---

## 2. 问题诊断与成因分析

通过系统日志和注册表排查，发现了多处 Windows 网络栈异常：

### 2.1 Ghost 设备与多余适配器残留

系统里存在多个废弃的 Wintun 虚拟适配器：

- 正在使用的活动设备：`SWD\WINTUN\{9E6FE1DA-...}` (**Meta Tunnel**)
- 废弃的 Ghost 设备：`ROOT\WINTUN\0000` (Wintun Userspace Tunnel)、`ROOT\WINTUN\0001` (Wintun Userspace Tunnel #2)

### 2.2 NetBT 4311 报错

Windows 系统日志中频繁出现 `NetBT` 来源的事件 ID `4311` 警告：

> **无法初始化设备, 初始化失败。NetBT 无法将全局唯一接口标识(GUID)映射到 MAC 地址, 该接口表示一个失败的接口。**

**根本原因**：多余的废弃 Wintun 适配器没有 MAC 地址，当 Windows 尝试为其初始化 NetBT 时发生异常，导致整个 Windows 网络栈出现混乱，阻塞了正常的流量转发。

---

## 3. 修复尝试记录（失败与成功）

整个修复过程经历了两个阶段：从**根因级清理尝试（失败）到自动化守护进程修复（成功）**。

### 3.1 阶段一：注册表清理与 Ghost 设备删除（失败）

我们尝试通过直接清理网络栈中的垃圾项来彻底根治此问题。

#### 实施方案

编写了 [fix-tun-definitive.ps1](file:///C:/Users/Richard/.gemini/antigravity/scratch/fix-tun-definitive.ps1) 和 [fix-tun-remove-wintun.ps1](file:///C:/Users/Richard/.gemini/antigravity/scratch/fix-tun-remove-wintun.ps1) 脚本，尝试执行以下操作：

1. 删除 `ROOT\WINTUN\0000` 和 `ROOT\WINTUN\0001` 废弃设备。
1. 清理 `HKLM:\System\CurrentControlSet\Services\NetBT\Parameters\Interfaces` 中残留的孤儿接口 GUID。
1. 清理已不存在的“本地连接*”等孤儿网络适配器连接项。
1. 将网络连接管理器服务（Netman）设为自动启动。

#### 实施过程中的插曲 (脚本语法错误)

在首次运行 `fix-tun-definitive.ps1` 时遇到了几处 PowerShell 语法和编码问题：

- **问题 1**：第 25 行 `$ErrorActionPreference = "Continue"` 附近报 `函数参数列表中缺少“)”` 错误。
- **问题 2**：第 192 行出现中文乱码（Mojibake），导致 `表达式中缺少右“)”` 错误。
- **问题 3**：第 380 行在进行字符串截取 `[Math]::Min` 时，由于语法嵌套错误报 `必须在“..”运算符后面提供一个值表达式`。
- **修复方法**：重新修正脚本的括弧配对，统一使用 UTF-8 编码保存脚本以消除乱码，再次测试后顺利运行。

#### 结果与结论

- **结果**：多余的 Wintun 适配器成功被删除，垃圾注册表也被清理干净。**但是，重启电脑后问题依然存在**。
- **结论**：即使清理了所有的残留项，Windows 11 在创建新的 Mihomo/Meta Tunnel 虚拟网卡时，仍会概率性触发网络共享和路由表的锁死。该锁死属于 Windows 网络栈深层 Bug，无法仅通过静态清理来预防。

---

### 3.2 阶段二：自动化静默 ICS 开关计划任务（成功）

既然无法防止 Windows 锁死，我们决定采用**自动化代替人工操作**的策略，在开机或 TUN 启动时自动执行 ICS 开关操作。

#### 实施方案

编写了自动修复脚本 [tun-autofix.ps1](file:///C:/Users/Richard/.gemini/antigravity/scratch/tun-autofix.ps1) 与安装脚本 [install-autofix.ps1](file:///C:/Users/Richard/.gemini/antigravity/scratch/install-autofix.ps1)。

#### 运行机制

```
graph TD
    A[开机/用户登录] --> B[静默启动后台任务 tun-autofix.ps1]
    B --> C{检测 Mihomo/Wintun 网卡是否在线?}
    C -- 否 (等待最多120秒) --> C
    C -- 是 --> D{检测当前网络是否通畅? ping 223.5.5.5}
    D -- 网络已通 --> E[记录日志并安全退出]
    D -- 网络断开 --> F[通过 COM 接口 HNetCfg.HNetShare 获取物理网卡]
    F --> G[开启 ICS 共享 -> 等待2秒 -> 关闭 ICS 共享]
    G --> H[刷新 DNS 缓存 ipconfig /flushdns]
    H --> I{验证网络是否恢复?}
    I -- 成功 --> J[写入 SUCCESS 日志并退出]
    I -- 失败 (重试最多4次) --> H

```

#### 安装命令

以**管理员权限**打开 PowerShell，执行以下命令：

```
Set-ExecutionPolicy -Scope CurrentUser Bypass -Force
& "C:\Users\Richard\.gemini\antigravity\scratch\install-autofix.ps1"

```

该脚本会在 Windows 计划任务中注册一个名为 **`ClashParty-TUN-AutoFix`** 的任务：

- **触发器**：用户登录时。
- **权限**：使用管理员最高权限（Highest RunLevel）运行。
- **交互性**：隐藏窗口静默运行，用户无感知。

#### 验证结果

- 重启电脑后，脚本在后台静默运行。
- 观察日志文件 `%LOCALAPPDATA%\ClashTunFix\fix.log`，可以看到任务成功检测到网络未通并完成了 ICS 开关。
- **最终效果**：重启后无需任何手动干预，网络在开机后 5-10 秒内自动恢复正常，TUN 模式完全可用。

---

## 4. 新增问题与优化记录 (2026-07-15)

### 4.1 新问题：开机自动运行提示“搜索不到特定网络”

- **现象**：开机后计划任务自动运行，但会弹窗提示“搜索不到特定网络”（或直接报错退出），需要用户在 Clash Party 中手动关闭并重新开启 TUN 模式，然后手动运行 fix 脚本才能恢复网络。
- **原因分析**：
  1. 开机或用户登录时，Clash Party 尚未完全启动或尚未初始化 TUN 网卡。
  1. 原始脚本 `fix-clash-tun.ps1` 没有任何等待逻辑，直接执行一次检测，若此时检测不到 TUN 适配器便会立刻报错退出。
  1. 因为原计划任务启动的是 `.bat` 文件且含有 `pause` 阻断，导致后台挂起多个未退出的 `cmd.exe` 进程，且弹窗阻塞。

### 4.2 解决方案与改进

1. **引入延迟等待逻辑**：在 [fix-clash-tun.ps1](file:///D:/Documents/Codex%20Project/fix-clash-tun.ps1) 中加入了最大 120 秒的循环等待机制（每 3 秒检测一次），直到 TUN 适配器和物理网卡均处于 `Up` 状态后再继续执行，彻底解决启动时序问题。
1. **新增静默运行参数 `-Silent`**：在计划任务中调用时传入 `-Silent` 参数，此时脚本不会进行 `ReadKey` 或 `pause` 等交互性阻断操作，也不会弹出任何可见窗口，直接将结果输出到本地日志 `%LOCALAPPDATA%\ClashTunFix\fix.log` 中。
1. **编写自提权更新脚本**：创建了 [update-task.ps1](file:///D:/Documents/Codex%20Project/update-task.ps1)，方便双击自动提权，一键将计划任务的执行动作直接指向 PowerShell 执行 `.ps1` 脚本，避免通过 `.bat` 启动带来的后台挂起进程问题。

---

## 5. 关键脚本文件清单与管理

### 5.1 脚本列表

- 📂 **自定义修复脚本（当前使用）**：[fix-clash-tun.ps1](file:///D:/Documents/Codex%20Project/fix-clash-tun.ps1)
- 📂 **计划任务更新脚本**：[update-task.ps1](file:///D:/Documents/Codex%20Project/update-task.ps1)
- 📂 **原自动修复脚本**：[tun-autofix.ps1](file:///C:/Users/Richard/.gemini/antigravity/scratch/tun-autofix.ps1)
- 📂 **原安装脚本**：[install-autofix.ps1](file:///C:/Users/Richard/.gemini/antigravity/scratch/install-autofix.ps1)

### 5.2 日志排查

日志保存在本地：`%LOCALAPPDATA%\ClashTunFix\fix.log`。可在 PowerShell 中运行以下命令查看：

```
Get-Content "$env:LOCALAPPDATA\ClashTunFix\fix.log" -Tail 30

```

### 5.3 卸载方法

如果今后不再需要此自动修复，可以随时在管理员 PowerShell 中将其卸载：

```
Unregister-ScheduledTask -TaskName 'Clash tun fix' -Confirm:$false

```
